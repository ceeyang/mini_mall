# 项目目录结构说明

本文档详细说明 Mini Mall 项目的目录结构。

## 📁 整体结构

```
mini_mall/
├── frontend/          # 前端代码目录
├── backend/           # 后端代码目录（预留）
├── doc/               # 文档目录
├── 配置文件           # 部署和项目配置
└── 根目录文件         # README、.gitignore 等
```

## 🎨 前端目录结构

### frontend/

前端所有代码和资源都在此目录下。

```
frontend/
├── *.html             # 所有 HTML 页面文件
├── package.json       # 前端项目配置
├── package-lock.json  # 依赖锁定文件
└── src/               # 前端源代码
    ├── components/    # UI 组件
    ├── scripts/       # JavaScript 模块
    ├── styles/        # 样式文件
    ├── data/          # 数据文件
    └── assets/        # 静态资源
```

### 页面文件说明

- `index.html` - 首页
- `products.html` - 商品列表页
- `cart.html` - 购物车页
- `checkout.html` - 结算页
- `login.html` - 登录/注册页
- `user-center.html` - 用户中心/订单查询页
- `about.html` - 关于我们页
- `contact.html` - 联系我们页

### 源代码目录说明

#### src/components/
所有 UI 组件模块，每个组件负责一个功能模块的渲染和交互。

#### src/scripts/
JavaScript 业务逻辑模块：
- `main.js` - 首页入口
- `*-page.js` - 各页面入口文件
- `auth.js` - 用户认证管理
- `cart.js` - 购物车管理
- `orders.js` - 订单管理
- `payment.js` - 支付服务
- `ui.js` - UI 交互工具

#### src/styles/
CSS 样式文件：
- `main.css` - 主样式文件，包含全局样式和自定义样式

#### src/data/
静态数据文件：
- `products.js` - 商品数据

#### src/assets/
静态资源文件：
- `images/` - 图片资源

## ⚙️ 后端目录结构（预留）

### backend/

后端代码目录，目前为空，等待后端开发时使用。

```
backend/
├── src/              # 后端源代码
│   ├── app.js       # 应用入口
│   ├── server.js    # 服务器配置
│   └── ...
├── config/           # 配置文件
│   ├── database.js  # 数据库配置
│   └── ...
├── models/          # 数据模型
│   ├── User.js      # 用户模型
│   ├── Product.js   # 商品模型
│   ├── Order.js     # 订单模型
│   └── ...
├── routes/          # 路由文件
│   ├── auth.js      # 认证路由
│   ├── products.js  # 商品路由
│   ├── orders.js    # 订单路由
│   └── ...
├── middleware/      # 中间件
│   ├── auth.js      # 认证中间件
│   └── ...
├── database/        # 数据库相关
│   ├── migrations/  # 数据库迁移
│   └── ...
└── README.md        # 后端说明文档
```

## 📚 文档目录

### doc/

所有项目文档都放在此目录下：

- `DEPLOYMENT.md` - 详细部署文档
- `QUICKSTART.md` - 快速开始指南
- `ARCHITECTURE.md` - 架构文档
- `PAYMENT_INTEGRATION.md` - 支付接入指南
- `GOOGLE_OAUTH.md` - Google OAuth 接入指南
- `DEPLOY_NOW.md` - 立即部署指南

## 🔧 配置文件

### 根目录配置文件

- `vercel.json` - Vercel 部署配置（指向 frontend 目录）
- `netlify.toml` - Netlify 部署配置（指向 frontend 目录）
- `.gitignore` - Git 忽略文件配置
- `.cursorrules` - Cursor 规则文件
- `deploy.sh` - 部署辅助脚本

## 🚀 开发工作流

### 前端开发

```bash
# 进入前端目录
cd frontend

# 安装依赖（可选）
npm install

# 启动开发服务器
npm run dev
# 或
python3 -m http.server 8000
```

### 后端开发（未来）

```bash
# 进入后端目录
cd backend

# 安装依赖
npm install
# 或
pip install -r requirements.txt

# 启动开发服务器
npm run dev
# 或
python app.py
```

## 📝 路径引用规范

### 前端内部引用

- HTML 文件引用资源：使用相对路径 `src/styles/main.css`
- JavaScript 模块引用：使用相对路径 `../components/navbar.js`
- 页面间跳转：使用相对路径 `products.html`

### 部署配置

- Vercel：配置 `rootDirectory` 为 `frontend`
- Netlify：配置 `publish` 为 `frontend`
- GitHub Pages：需要将 `frontend` 目录内容作为根目录

## 🔄 扩展性说明

### 添加新页面

1. 在 `frontend/` 目录创建新的 HTML 文件
2. 在 `frontend/src/components/` 创建对应组件
3. 在 `frontend/src/scripts/` 创建页面入口文件

### 添加后端功能

1. 在 `backend/` 目录创建相应的模块
2. 按照目录结构组织代码
3. 更新 `backend/README.md` 说明新功能

### 添加新文档

所有新文档都应放在 `doc/` 目录下，遵循命名规范。

---

**遵循此目录结构，保持项目清晰和可维护！** 🎉
